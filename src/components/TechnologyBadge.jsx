export default function TechnologyBadge({title}){
    return(
        <span className="font-mono text-[10px] tracking-[.06em] text-accent bg-accent/10 border border-accent/30 px-[10px] py-[3px]">{title}</span>
    )
}